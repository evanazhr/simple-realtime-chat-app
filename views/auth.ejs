<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= register ? "Register" : "Login" %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet" />
    <style>
      body {
        font-family: 'Nunito', sans-serif;
      }
    </style>
  </head>
  <body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen px-4">
    <div class="bg-gray-800 w-full max-w-sm p-8 rounded-xl shadow-lg text-center">
      <h2 class="text-2xl font-bold text-teal-400 mb-6">
        <%= register ? "Register" : "Login" %> to Chat
      </h2>

      <% if (error) { %>
      <p class="<%= error ? 'text-red-500' : 'text-teal-500' %> font-semibold mb-4">
        <%= error %>
      </p>
      <% } %>

      <form method="POST" action="<%= register ? '/register' : '/login' %>" class="flex flex-col gap-5">
        <input
          name="username"
          type="text"
          placeholder="Username"
          required
          autocomplete="username"
          class="bg-gray-700 border border-gray-600 focus:border-teal-400 focus:ring-2 focus:ring-teal-500 text-white rounded-md px-4 py-3 placeholder-gray-400 focus:outline-none transition"
        />
        <input
          name="password"
          type="password"
          placeholder="Password"
          required
          autocomplete="<%= register ? 'new-password' : 'current-password' %>"
          class="bg-gray-700 border border-gray-600 focus:border-teal-400 focus:ring-2 focus:ring-teal-500 text-white rounded-md px-4 py-3 placeholder-gray-400 focus:outline-none transition"
        />
        <button
          type="submit"
          class="bg-teal-500 hover:bg-teal-600 text-gray-900 font-bold py-3 rounded-md transition"
        >
          <%= register ? "Register" : "Login" %>
        </button>
      </form>

      <% if (!register) { %>
      <p class="text-sm text-gray-400 mt-5">
        Don't have an account?
        <a href="/register" class="text-teal-400 font-semibold hover:underline hover:text-teal-300">
          Register here
        </a>
      </p>
      <% } else { %>
      <p class="text-sm text-gray-400 mt-5">
        Have an account?
        <a href="/login" class="text-teal-400 font-semibold hover:underline hover:text-teal-300">
          Login here
        </a>
      </p>
      <% } %>
    </div>
  </body>
</html>
